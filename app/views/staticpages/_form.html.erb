<%= form_with(model: staticpage) do |form| %>
  <% if staticpage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(staticpage.errors.count, "error") %> prohibited this staticpage from being saved:</h2>

      <ul>
        <% staticpage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :flickr_id, style: "display: block" %>
    <%= form.text_field :flickr_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
